class Beer < ActiveRecord::Base
    belongs_to :brewery
    has_many :ratings

    def average_rating
        sum = ratings.inject({}) do |sum, rating|
            sum + rating.score
            sum
        end
        sum / ratings.count.to_f
    end
end
