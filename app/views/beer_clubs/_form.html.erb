<%= form_for(@beer_club) do |f| %>
  <% if @beer_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
      <% @beer_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-5">
    <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class:'form-control' %>
    <%= f.label :founded %>
    <%= f.number_field :founded, class:'form-control' %>
    <%= f.label :city %>
    <%= f.text_field :city, class:'form-control' %>
    </div>
    <%= f.submit 'Create beer club', class:'btn btn-primary' %>
  </div>
<% end %>
